# 盲人导航安卓应用 - 产品需求文档 (PRD)

## 1. 项目信息

- **Language**: 中文
- **Programming Language**: Kotlin + Android Native
- **Project Name**: blindnav_android
- **原始需求**: 将 Python 智能眼镜盲人导航项目移植为安卓手机应用，去掉云端 API 依赖，使用手机摄像头，以手动点击交互替代语音交互，输出可安装 APK。

---

## 2. 产品定义

### 2.1 Product Goals

1. **独立离线运行**：将原 Python 后端的视觉检测能力（YOLO + MediaPipe）移植到安卓端，无需服务器或云端 API，所有推理在设备端完成
2. **核心导航功能保留**：保留盲道导航、过马路辅助、红绿灯检测、物品识别四大核心视觉功能，通过预录音频提供语音反馈
3. **替代交互模式**：用**按钮点击 + 滑动操作**替代原项目的语音指令交互（原 ASR/Qwen API 已移除），保证视障用户可通过触觉操作独立使用

### 2.2 User Stories

1. **As a 视障用户**, I want to 点击"开始盲道导航"按钮启动盲道检测 so that 我可以在手机摄像头引导下沿盲道行走，听到左转/右转/直行的语音提示
2. **As a 视障用户**, I want to 点击"过马路"按钮进入过马路模式 so that 系统能检测斑马线和红绿灯，引导我安全通过马路
3. **As a 视障用户**, I want to 点击"物品查找"并输入目标物品名称 so that 系统能在画面中检测该物品并用语音引导我手部靠近抓取
4. **As a 视障用户**, I want to 在导航过程中听到清晰的语音警告（"前方有障碍物"、"红灯"等） so that 我能及时做出反应避免危险
5. **As a 视障用户**, I want to 通过大按钮和触觉反馈控制导航 so that 我无需看屏幕也能操作手机

---

## 3. Requirements Pool

### P0 - Must Have（核心功能，必须实现）

| ID | 需求 | 说明 |
|----|------|------|
| P0-01 | 摄像头权限申请 | APK 首次启动必须申请 CAMERA 权限，无权限时引导用户到设置页 |
| P0-02 | 手机摄像头实时取景 | 使用 CameraX 替代 ESP32-CAM，获取实时视频帧用于推理 |
| P0-03 | 盲道检测与导航 | 将 yolo-seg.pt 转换为 ONNX/TFLite，在安卓端实时分割盲道，输出方向指引（左转/右转/直行） |
| P0-04 | 障碍物检测与避障 | 将 yoloe-11l-seg.pt 转换为 ONNX/TFLite，检测前方障碍物并发出语音警告 |
| P0-05 | 预录音频播放系统 | 将 voice/ 和 music/ 中的 wav 文件打包到 assets，建立文本→音频映射，替代原 TTS 系统 |
| P0-06 | 主界面导航控制 | 大按钮布局：开始盲道导航 / 过马路 / 物品查找 / 停止，每个按钮≥80dp，支持无障碍描述 |
| P0-07 | 相机预览画面 | 全屏预览摄像头画面，叠加检测结果可视化（盲道掩码、障碍物框、方向箭头） |
| P0-08 | 摄像头翻转适配 | 支持前置/后置摄像头切换，默认后置（面向道路） |

### P1 - Should Have（重要功能，应该实现）

| ID | 需求 | 说明 |
|----|------|------|
| P1-01 | 过马路辅助 | 将 workflow_crossstreet.py 移植：斑马线分割检测 + 红绿灯检测（trafficlight.pt），提供对齐引导和通行提示 |
| P1-02 | 红绿灯状态检测 | 使用 trafficlight.pt 模型检测红/绿/黄灯状态，语音播报当前灯色 |
| P1-03 | 物品查找模式 | 将 yolomedia.py 核心逻辑移植：YOLO-E 开放词汇检测 + ByteTrack 追踪 + MediaPipe 手部引导 |
| P1-04 | 手势/按钮输入物品名 | 物品查找时提供文本输入框（或语音输入备选），输入目标物品名称 |
| P1-05 | 转弯检测 | 盲道导航中检测急转弯，提前语音提醒"前方有左转弯" |
| P1-06 | MediaPipe 手部检测 | 使用 MediaPipe Android SDK 实现手部检测，引导用户靠近物品 |
| P1-07 | 障碍物名称语音播报 | 障碍物检测时用中文语音告知障碍物类型（"前方有人"、"右侧有车"等），复用预录 wav |
| P1-08 | 导航状态机 | 移植 NavigationMaster 状态机，管理 IDLE/导航/过马路/找物品 等状态切换 |
| P1-09 | 光流稳定 | 移植 Lucas-Kanade 光流算法，稳定盲道掩码减少抖动 |

### P2 - Nice to Have（锦上添花）

| ID | 需求 | 说明 |
|----|------|------|
| P2-01 | 无障碍优化 | TalkBack 完整适配，所有控件有 contentDescription |
| P2-02 | 深色模式 | 系统级深色模式适配 |
| P2-03 | 手电筒控制 | 导航模式下可开启手机闪光灯辅助照明 |
| P2-04 | 振动反馈 | 关键事件（障碍物警告、到达转弯点）提供振动反馈 |
| P2-05 | 模型首次启动下载 | 大型模型文件不打包进 APK，首次启动时从服务器下载到本地 |
| P2-06 | 录制功能 | 移植 sync_recorder，可录制导航过程的音视频 |
| P2-07 | 手势控制 | 自定义手势（如双击启动导航、滑动切换模式）替代部分按钮操作 |

---

## 4. UI Design Draft

### 4.1 主界面布局

```
┌──────────────────────────────────┐
│          相机预览区域              │  ← 全屏 CameraX Preview
│  ┌────────────────────────────┐  │     叠加检测结果（盲道掩码、
│  │                            │  │     障碍物框、方向箭头等）
│  │     实时视频 + 检测叠加      │  │
│  │                            │  │
│  └────────────────────────────┘  │
│                                  │
│  ┌────────────────────────────┐  │
│  │   状态栏：当前模式 + 文字提示  │  ← "盲道导航中 - 请直行"
│  └────────────────────────────┘  │
│                                  │
│  ┌────────┐  ┌────────┐         │
│  │ 盲道导航 │  │ 过马路  │         │  ← P0 大按钮区域
│  └────────┘  └────────┘         │     每个按钮 ≥ 80dp
│  ┌────────┐  ┌────────┐         │     圆角、高对比度配色
│  │ 物品查找 │  │  停止   │         │     语音描述 contentDescription
│  └────────┘  └────────┘         │
│                                  │
│  ┌────────────────────────────┐  │
│  │  关闭摄像头 / 切换前后置     │  │  ← 辅助控制栏
│  └────────────────────────────┘  │
└──────────────────────────────────┘
```

### 4.2 物品查找输入界面

```
┌──────────────────────────────────┐
│  ← 返回                          │
│                                  │
│  请输入要查找的物品：              │
│  ┌────────────────────────────┐  │
│  │ 例：红牛、矿泉水、手机       │  │  ← 大字体输入框
│  └────────────────────────────┘  │
│                                  │
│  常用物品（快捷按钮）：            │
│  [红牛] [矿泉水] [手机] [钥匙]    │  ← 预设快捷入口
│                                  │
│        [ 开始查找 ]               │
└──────────────────────────────────┘
```

### 4.3 导航过程中的界面

- **全屏相机预览**：占据主要区域
- **底部状态栏**：半透明叠加，显示当前模式和文字提示
- **语音播报**：自动播放预录音频（通过 AudioManager 管理音频焦点）
- **紧急停止按钮**：悬浮在右下角，随时可停止导航

### 4.4 交互说明

| 原项目交互 | 安卓替代方案 |
|-----------|------------|
| 语音说"开始导航" | 点击"盲道导航"按钮 |
| 语音说"开始过马路" | 点击"过马路"按钮 |
| 语音说"帮我找XX" | 点击"物品查找" → 输入物品名 → 点击"开始查找" |
| 语音说"停止导航" | 点击"停止"按钮 |
| 语音说"找到了" | 点击"找到了"确认按钮（物品查找模式中出现） |
| ASR 实时语音反馈 | 预录音频 wav 自动播放 |
| Qwen 多模态对话 | **移除**（不需要） |

---

## 5. 技术架构概要

### 5.1 技术栈

| 层级 | 技术选择 |
|------|---------|
| 语言 | Kotlin |
| UI 框架 | Jetpack Compose + Material 3 |
| 相机 | CameraX |
| 模型推理 | ONNX Runtime Android（YOLO 模型） / TFLite（备选） |
| 手部检测 | MediaPipe Android SDK（hand_landmarker） |
| 目标追踪 | ByteTrack（需要自行移植或使用简化版） |
| 音频播放 | MediaPlayer / SoundPool |
| 构建工具 | Gradle + AGP |

### 5.2 模型文件处理

| 原始模型 | 安卓格式 | 用途 | 预估大小 |
|---------|---------|------|---------|
| yolo-seg.pt | .onnx | 盲道+斑马线分割 | ~50MB |
| yoloe-11l-seg.pt | .onnx | 开放词汇检测（障碍物/物品） | ~80MB |
| shoppingbest5.pt | .onnx | 物品识别专用 | ~30MB |
| trafficlight.pt | .onnx | 红绿灯检测 | ~20MB |
| hand_landmarker.task | 原生格式 | 手部检测（MediaPipe 直接使用） | ~15MB |

### 5.3 需要从原项目复制的资源

- `voice/` 目录所有 wav 文件 + `map.zh-CN.json` → 打包到 `assets/voice/`
- `music/` 目录 wav 文件 → 打包到 `assets/music/`
- 模型文件 → 转换后打包到 `assets/models/` 或首次下载

---

## 6. Open Questions（待确认）

1. **模型性能**：YOLO 大模型（yoloe-11l-seg.pt ~80MB）在安卓手机上的推理速度如何？是否需要蒸馏/量化到更小的模型？目标帧率是多少（建议≥15fps）？

2. **模型转换**：yolo-seg.pt 和 yoloe-11l-seg.pt 需要从 PyTorch 转换为 ONNX，原项目的 YOLO 后处理逻辑（NMS、掩码解码等）需要在安卓端用 Kotlin 重写，工作量需要评估。

3. **YOLO-E 开放词汇检测**：yoloe-11l-seg.pt 使用文本提示（text prompt）进行检测，ONNX 导出后文本编码器如何处理？是否需要额外的 CLIP 文本编码器模型？

4. **ByteTrack 移植**：原项目使用的 ByteTrack 追踪算法需要自行移植到安卓，还是可以用 MediaPipe 的 ObjectTracker 替代？

5. **最低安卓版本**：建议最低支持 Android 8.0 (API 26)？还是需要更低版本兼容？

6. **目标手机硬件**：是否需要考虑没有 GPU/NPU 的低端手机？是否做 CPU-only 模式的兼容？

7. **物品查找模式的手部引导**：原项目使用 MediaPipe + 自定义光流追踪，在安卓上 MediaPipe SDK 原生支持是否足够？光流追踪部分是否需要移植？

8. **音频焦点管理**：导航语音播报是否会与用户的音乐/TalkBack 冲突？需要设计音频优先级策略。

---

*文档版本: v1.0 | 日期: 2025-07-08 | 作者: Alice (Product Manager)*
